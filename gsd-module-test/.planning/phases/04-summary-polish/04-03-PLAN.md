---
phase: 04-summary-polish
plan: 03
type: execute
wave: 2
depends_on: [04-01, 04-02]
files_modified:
  - src/App.tsx
  - src/components/SummaryPanel/SummaryPanel.test.tsx
  - src/components/SubtotalsStrip/SubtotalsStrip.test.tsx
autonomous: false
requirements: [SUMM-01, SUMM-02, SUMM-03]

must_haves:
  truths:
    - "On a 375px wide screen, panels stack vertically with no horizontal scroll"
    - "People+Items row collapses to single column on mobile"
    - "Touch targets on interactive elements are at least 44px tall"
    - "Edge cases (zero people, one person, all unassigned, zero tip/tax) render without errors"
  artifacts:
    - path: "src/App.tsx"
      provides: "Responsive layout with flex-col sm:flex-row on panels row"
    - path: "src/components/SummaryPanel/SummaryPanel.test.tsx"
      provides: "Edge-case smoke tests"
    - path: "src/components/SubtotalsStrip/SubtotalsStrip.test.tsx"
      provides: "Edge-case smoke tests"
  key_links:
    - from: "src/App.tsx"
      to: "all panel components"
      via: "flex-col sm:flex-row responsive class"
      pattern: "flex-col sm:flex-row"
---

<objective>
Apply mobile-responsive layout (single-column stack at mobile widths) and add edge-case smoke tests to verify all SUMM requirements hold for boundary conditions. Final checkpoint for visual verification.

Purpose: Ensures the app is usable on a 375px phone screen (ROADMAP success criterion 4) and edge cases render without errors (criterion 5). Covers all three SUMM requirements for verification completeness.
Output: Responsive App.tsx layout, edge-case smoke tests, visual checkpoint confirmation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-summary-polish/04-01-SUMMARY.md
@.planning/phases/04-summary-polish/04-02-SUMMARY.md

@src/App.tsx
@src/components/SummaryPanel/SummaryPanel.tsx
@src/components/SummaryPanel/SummaryPanel.test.tsx
@src/components/SubtotalsStrip/SubtotalsStrip.tsx
@src/components/SubtotalsStrip/SubtotalsStrip.test.tsx

<interfaces>
<!-- From Plan 01 and 02 outputs -->

From src/App.tsx (after Plan 02):
```typescript
// Layout: panels row -> SubtotalsStrip -> ChargesPanel -> SummaryPanel
// The flex row for People+Items needs responsive classes added
<div className="flex gap-6 mb-6">
  <PeoplePanel />
  <ItemsPanel />
</div>
```

From src/components/SummaryPanel/SummaryPanel.tsx:
```typescript
export function SummaryPanel()  // table with Name|Subtotal|Tip|Tax|Total
```

From src/components/SubtotalsStrip/SubtotalsStrip.tsx:
```typescript
export function SubtotalsStrip()  // returns null when 0 people
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mobile responsive layout and touch targets</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx (current className on the flex row — must see exact current state after Plan 02 wiring)
    - src/components/ChargesPanel/TipControl.tsx (check if Button uses size="sm" — may need min-h-[44px] for touch targets)
    - src/components/ChargesPanel/TaxControl.tsx (check Button sizes)
    - src/components/PeoplePanel/PeoplePanel.tsx (check if it has flex-1 or width classes)
    - src/components/ItemsPanel/ItemsPanel.tsx (check if it has flex-1 or width classes)
  </read_first>
  <action>
    1. In `src/App.tsx`, change the People+Items container div from:
    ```
    <div className="flex gap-6 mb-6">
    ```
    to (per D-06, D-07):
    ```
    <div className="flex flex-col sm:flex-row gap-6 mb-6">
    ```
    This makes the panels stack vertically by default (mobile) and go side-by-side at sm: (640px+). Per Research open question #1, `sm:` at 640px is acceptable — it covers all phones. The `flex-col` ensures each panel takes full width on mobile.

    2. On the outer container `<div className="min-h-screen bg-background p-8">`, change `p-8` to `p-4 sm:p-8` so mobile has tighter padding (32px -> 16px) giving more content room on 375px screens.

    3. Touch target sizing: Check TipControl.tsx and TaxControl.tsx for `size="sm"` on Button components. If any Button has `size="sm"`, add `className="min-h-[44px]"` to those Buttons. If Buttons already meet 44px (size="default" is typically 40px + border), add `min-h-[44px]` to be safe. This is Claude's Discretion per CONTEXT.md.

    Note: Do NOT change SubtotalsStrip or SummaryPanel — they are full-width by default and already stack correctly.
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - src/App.tsx contains `flex flex-col sm:flex-row` on the People+Items container div
    - src/App.tsx contains `p-4 sm:p-8` on the outer container div
    - `npx vitest run` exits 0 (no regressions)
  </acceptance_criteria>
  <done>Layout collapses to single column on mobile (< 640px), side-by-side on desktop. Padding reduces on mobile. Touch targets meet 44px minimum.</done>
</task>

<task type="auto">
  <name>Task 2: Edge-case smoke tests for all SUMM requirements</name>
  <files>src/components/SummaryPanel/SummaryPanel.test.tsx, src/components/SubtotalsStrip/SubtotalsStrip.test.tsx</files>
  <read_first>
    - src/components/SummaryPanel/SummaryPanel.test.tsx (existing tests from Plan 01 — add to, don't duplicate)
    - src/components/SubtotalsStrip/SubtotalsStrip.test.tsx (existing tests from Plan 02 — add to, don't duplicate)
    - src/components/SummaryPanel/SummaryPanel.tsx (component implementation to understand what edge cases to hit)
    - src/components/SubtotalsStrip/SubtotalsStrip.tsx (component implementation)
    - src/store/useBillStore.ts (action APIs for state setup)
  </read_first>
  <action>
    1. Add edge-case smoke tests to `src/components/SummaryPanel/SummaryPanel.test.tsx` (only if not already covered by Plan 01):

    - Test: "renders with one person, no items" — add 1 person, no items, render => row shows all $0.00, no error thrown
    - Test: "renders with multiple people, mixed assigned/unassigned items" — add 3 people, add 3 items (1 assigned to person A, 1 shared between A+B, 1 unassigned), set tip 18% proportional, set tax amount 300 cents equal => renders without error, sum of Total column values equals expected total
    - Test: "SUMM-02 balance: sum of displayed totals equals bill total" — add 3 people, add items totaling $33.33 (3333 cents), assign all items, set tip 15% proportional, tax 8.5% proportional => extract all total cells, parse values, verify sum equals expected

    2. Add edge-case smoke tests to `src/components/SubtotalsStrip/SubtotalsStrip.test.tsx` (only if not already covered by Plan 02):

    - Test: "handles many people without overflow" — add 5 people, add 1 item shared among all 5 => all 5 names rendered, no error
    - Test: "person with no assigned items shows $0.00" — add Alice and Bob, add item assigned only to Alice => Bob shows "$0.00"

    3. Ensure all new tests follow the existing pattern: `beforeEach(() => useBillStore.getState().resetBill())`, state setup via `useBillStore.getState().addPerson(...)` etc.
  </action>
  <verify>
    <automated>npx vitest run src/components/SummaryPanel/SummaryPanel.test.tsx src/components/SubtotalsStrip/SubtotalsStrip.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/components/SummaryPanel/SummaryPanel.test.tsx has at least 8 total `it(` calls (Plan 01 tests + new edge cases)
    - src/components/SubtotalsStrip/SubtotalsStrip.test.tsx has at least 6 total `it(` calls (Plan 02 tests + new edge cases)
    - `npx vitest run` exits 0 (full suite green)
  </acceptance_criteria>
  <done>Edge-case smoke tests verify zero-people, one-person, mixed-assignment, and balance-sum scenarios all render without error. Full suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete Phase 4 delivery</name>
  <files>n/a</files>
  <action>Human verifies the full Phase 4 delivery visually in the browser.</action>
  <what-built>
    Complete Phase 4 delivery: SummaryPanel with per-person breakdown table, SubtotalsStrip with live item subtotals, mobile responsive layout, edge-case handling. All three SUMM requirements implemented.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open the app in your browser
    2. Add 2-3 people (e.g., Alice, Bob, Charlie)
    3. Add 2-3 items with prices (e.g., Pizza $12.50, Salad $8.75, Drinks $15.00)
    4. Assign items: one to Alice only, one shared between Alice+Bob, one to all three
    5. Verify the **Subtotals strip** between Items and Charges shows each person's item subtotal updating live
    6. Set Tip to 20% proportional, Tax to $5.00 equal
    7. Verify the **Summary table** at the bottom shows 5 columns: Name | Subtotal | Tip | Tax | Total
    8. Verify the totals look reasonable (subtotals + tip shares + tax shares = total per person)
    9. Open DevTools, toggle device toolbar, set width to 375px
    10. Verify panels stack vertically: People, Items, Subtotals, Charges, Summary — no horizontal scrollbar
    11. Remove all people — verify empty state "No people added yet." in Summary, subtotals strip disappears
  </how-to-verify>
  <verify>Human visual confirmation</verify>
  <done>User approves that SummaryPanel, SubtotalsStrip, mobile layout, and edge cases all work correctly.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
npx vitest run
</verification>

<success_criteria>
- Mobile layout stacks vertically at < 640px with no horizontal scroll
- Edge-case smoke tests pass for boundary conditions
- User confirms visual correctness of SummaryPanel table, SubtotalsStrip, and mobile layout
</success_criteria>

<output>
After completion, create `.planning/phases/04-summary-polish/04-03-SUMMARY.md`
</output>
